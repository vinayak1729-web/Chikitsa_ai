{
    "build": {
      "command": "python -m pip install -r requirements.txt && python app.py",
      "output": "dist"
    },
    "routes": [
      {
        "src": "/(.*)",
        "dest": "dist/index.html"
      }
    ]
  }